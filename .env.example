# 服务端口（非常见端口，避免与本机其它服务冲突）
PORT=7763

# JWT
JWT_SECRET=ancient-tree-dev-secret-change-me
JWT_EXPIRES_IN=12h

# MySQL 连接（本地开发：通过 docker-compose 暴露在 3309）
DB_HOST=127.0.0.1
DB_PORT=3309
DB_USER=root
DB_PASSWORD=tree_root_pw
DB_NAME=ancient_tree

# 内置管理员（启动时自动创建）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
